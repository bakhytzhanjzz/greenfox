<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_blurred">

    <!-- Title -->
    <TextView
        android:text="Выберите тип пользователя"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:layout_marginBottom="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Subtitle -->
    <TextView
        android:text="Хотите отдохнуть или предоставить место для отдыха"
        android:textSize="16sp"
        android:textColor="#CCCCCC"
        android:gravity="center"
        android:layout_marginBottom="40dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Option Cards Container -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginBottom="40dp">

        <!-- Relax Option -->
        <RelativeLayout
            android:id="@+id/relax_option"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:background="@drawable/option_card_selector"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/client_image"
                android:scaleType="centerCrop"
                android:background="@drawable/rounded_corners"/>

            <!-- Overlay for text -->
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/overlay_gradient"/>

            <!-- Text at bottom -->
            <TextView
                android:text="Я хочу отдохнуть"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:gravity="center"
                android:padding="16dp"
                android:layout_alignParentBottom="true"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </RelativeLayout>

        <!-- Host Option -->
        <RelativeLayout
            android:id="@+id/host_option"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:background="@drawable/option_card_selector"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/partner_image"
                android:scaleType="centerCrop"
                android:background="@drawable/rounded_corners"/>

            <!-- Overlay for text -->
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/overlay_gradient"/>

            <!-- Text at bottom -->
            <TextView
                android:text="Приглашаю на отдых"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:gravity="center"
                android:padding="16dp"
                android:layout_alignParentBottom="true"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>
        </RelativeLayout>
    </LinearLayout>

    <!-- Next Button -->
    <Button
        android:id="@+id/btn_next"
        android:text="Далее"
        android:textSize="18sp"
        android:textColor="#666666"
        android:background="@drawable/button_selector"
        android:enabled="false"
        android:layout_width="match_parent"
        android:layout_height="56dp"/>

</LinearLayout>